import React from "react";

type IPlanet = {
    planet: any
};

export const Planet = ({planet}: IPlanet): JSX.Element => {
    return (
        <React.Fragment>

        </React.Fragment>
    );
};

